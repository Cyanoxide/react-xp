import styles from "./Setting.module.scss";

const Setting = () => {
    return (
        <div className={`${styles.settings} flex flex-col h-full`}>
            Test
        </div>
    );
};

export default Setting;